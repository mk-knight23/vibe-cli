/**
 * Genkit integration removed.
 * Keeping this stub so any lingering imports resolve to a no-op.
 * Remove this file entirely later if desired.
 */
export const ai = {
  definePrompt() {
    throw new Error('Genkit removed');
  },
  defineFlow() {
    throw new Error('Genkit removed');
  },
};
