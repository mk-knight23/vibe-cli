/**
 * Commit message generation flow removed with Genkit.
 * Stub left to avoid breaking imports.
 */
export type GenerateCommitMessageInput = { diff: string };
export type GenerateCommitMessageOutput = { commitMessage: string };

export async function generateCommitMessage(
  _input: GenerateCommitMessageInput
): Promise<GenerateCommitMessageOutput> {
  throw new Error('generateCommitMessage flow removed (Genkit disabled)');
}
